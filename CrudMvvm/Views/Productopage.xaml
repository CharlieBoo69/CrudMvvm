<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="CrudMvvm.Views.Productopage"
             Title="{Binding TituloPagina}">
    <VerticalStackLayout Padding="15">
        <ActivityIndicator IsVisible="{Binding LoadingEsVisible}"
                           IsRunning="{Binding LoadingEsVisible}"
                           Color="Blue"
                           HeightRequest="30"
                           />
        <Label Text="Nombre del producto"/>
        <Entry Text="{Binding ProductoDTO.NombreProducto}" Margin="0,0,0,10"/>

        <Label Text="Detalle del producto"/>
        <Entry Text="{Binding ProductoDTO.Detalle}" Margin="0,0,0,10"/>

        <Label Text="Precio del producto"/>
        <Entry Text="{Binding ProductoDTO.Precio}" Keyboard="Numeric" Margin="0,0,0,10"/>

        <Label Text="Fecha de caducidad"/>
        <DatePicker Date="{Binding ProductoDTO.FechaCaducidad}" Margin="0,0,0,10"/>


        <Button Text="Guardar" Command="{Binding GuardarCommand}" BackgroundColor="Green"/>



    </VerticalStackLayout>
</ContentPage>